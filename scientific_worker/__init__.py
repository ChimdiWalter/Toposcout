"""TopoScout scientific worker — Cloud Run service wrapping real_lesion_model_v1.

Everything here is TopoScout-owned. The research tree is imported read-only
(locally) or vendored at container build time; it is never modified.
"""
